# 可选提示词参考

这份文档不是主流程必需文件。

当前项目的推荐用法仍然是直接运行 `main.py` 或 `entropy-note`，由程序自动完成大纲提取、节级判断、分阶段生成、落库与导出。这里保留这份文档，是为了在以下场景中提供一个可复用的人工提示词参考：

- 在 NotebookLM 网页端手动试探某个主题的表达方式
- 在改 `src/prompts/markdown_prompts.py` 前，先快速验证提示词方向
- 为特殊学科临时定制补充提问，而不想改动主链路
- 对比“自动流水线结果”和“手动提问结果”的差异

如果只是正常使用本仓库生成学习资料，可以跳过这份文档。

## 何时使用

- 需要快速验证一个新学科或新章节的提示词方向
- 需要单独追问概念、推导、图示或易错点
- 需要把自动生成之外的补充问答沉淀成自定义模板

## 何时不要使用

- 不要把这份文档当成主入口
- 不要优先手工复制粘贴提示词来替代现有流水线
- 不要让一次性的临时提示词污染 `README`、`AGENTS.md` 或核心代码

## 设计原则

为手动提示词补充问题时，建议尽量遵守以下原则：

- 先说明目标：是要理解机制、补推导、出图，还是做速查
- 明确输出结构：列表、表格、因果链、步骤拆解或图示说明
- 要求保留学习视角：强调直觉、主线、易错点，而不是只堆定义
- 数学或分析类主题中，要求补充必要公式，并使用标准 LaTeX
- 如果需要图示，明确说明是流程图、结构图还是曲线/坐标图

## 可复用模板

### 1. 机制主线梳理

适合概念多、关系复杂，但希望先抓主线的章节。

```text
请基于提供的资料，帮我整理这一主题的主线。不要先罗列定义，而是回答：
1. 这一章最核心的机制或问题是什么？
2. 各个概念之间是什么关系？
3. 如果变量 A 变化，会通过怎样的链条影响 B 和 C？
4. 最容易忽略的前提、边界条件或常见误解是什么？

请按“核心问题 -> 机制链条 -> 易错点”的结构输出。
```

### 2. 推导补充

适合自动主正文已经够通顺，但还想单独追问严谨推导。

```text
请围绕这个主题补充必要的公式与推导，但不要把正文写成纯公式堆砌。
要求：
1. 先用 2-3 句话解释推导目标；
2. 再分步骤给出关键公式；
3. 每一步说明变量含义与为什么这样变形；
4. 使用标准 LaTeX；
5. 最后总结结论成立的前提条件与常见误用。
```

### 3. 图示判断与图示说明

适合先判断“要不要图”，以及“应该画什么图”。

```text
请先判断这一节是否值得用图示辅助理解。
如果不需要，请直接说明原因。
如果需要，请继续回答：
1. 更适合曲线/坐标图、流程图、结构图，还是层级图？
2. 图中必须出现哪些变量、节点或关系？
3. 最关键的视觉重点是什么？
4. 这张图最容易画错的地方是什么？
```

### 4. 速查表补充

适合考前或复习时补一份压缩版资料。

```text
请把这一主题压缩成一份速查表，包含：
1. 核心概念及一句话解释；
2. 必要公式及变量含义；
3. 关键判断标准或分类框架；
4. 高频易错点；
5. 与相近概念的区别。

请优先保证可检索性和可复习性。
```

## 使用建议

- 若某个模板被反复使用，再考虑把它沉淀进 `src/prompts/markdown_prompts.py`
- 若只是单次试探，保留在这份文档或个人笔记里即可
- 若模板涉及图示判断，优先与 `visual_type` / `visual_ratio` 的主链路约定保持一致
